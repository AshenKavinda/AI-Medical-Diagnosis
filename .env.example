# Flask Configuration
FLASK_PORT=5000
FLASK_HOST=0.0.0.0
FLASK_DEBUG=True

# Model Configuration
# Default threshold for fuzzy matching (70-100)
DEFAULT_THRESHOLD=90

# Minimum probability to display predictions (0.0-1.0)
# Only predictions above this threshold will be shown
MIN_PROBABILITY=0.40

# Maximum number of predictions to return
MAX_PREDICTIONS=5

# Application Settings
APP_NAME=AI Medical Diagnosis System
APP_VERSION=1.0.0
